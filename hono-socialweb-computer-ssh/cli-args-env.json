{
  "name": "socialweb-computer-ssh",
  "description": "SSH front door and AT Protocol OAuth server for the compute market",
  "options": {
    "ssh-port": {
      "type": "number",
      "description": "TCP port for the SSH server (0 = ephemeral)",
      "env": "SSH_PORT",
      "default": 2222
    },
    "ssh-hostname": {
      "type": "string",
      "description": "Address the SSH server binds",
      "env": "SSH_HOSTNAME",
      "default": "127.0.0.1"
    },
    "http-port": {
      "type": "number",
      "description": "TCP port for the OAuth HTTP server",
      "env": "HTTP_PORT",
      "default": 8787
    },
    "serve-addr": {
      "type": "string",
      "description": "Address the OAuth HTTP server binds",
      "env": "SERVE_ADDR",
      "default": "127.0.0.1"
    },
    "state-dir": {
      "type": "string",
      "description": "Directory holding the OAuth session store, state store, and SSH host key",
      "env": "STATE_DIR",
      "default": ".socialweb-computer-ssh"
    },
    "host-key-path": {
      "type": "string",
      "description": "SSH host key path (generated if missing)",
      "env": "HOST_KEY_PATH"
    },
    "default-command": {
      "type": "string",
      "description": "Command run in the guest when the client requests a shell without a command",
      "env": "DEFAULT_COMMAND",
      "default": "bash"
    },
    "vm-ready-timeout-sec": {
      "type": "number",
      "description": "Seconds to wait for the provisioned guest's SSH to come up",
      "env": "VM_READY_TIMEOUT_SEC",
      "default": 300
    },
    "plc-directory-url": {
      "type": "string",
      "description": "PLC directory used to resolve handles and DIDs",
      "env": "PLC_DIRECTORY_URL",
      "default": "https://plc.directory"
    },
    "associations-cache-ttl-sec": {
      "type": "number",
      "description": "Seconds a successful key association lookup is cached",
      "env": "ASSOCIATIONS_CACHE_TTL_SEC",
      "default": 300
    },
    "label": {
      "type": "string",
      "description": "Service name used in structured logs",
      "env": "LABEL",
      "default": "socialweb-computer-ssh"
    },
    "web-dir": {
      "type": "string",
      "description": "Directory served as the web UI",
      "env": "WEB_DIR",
      "default": "web"
    },
    "max-sessions": {
      "type": "number",
      "description": "Concurrent provisioning runs allowed across all clients; further exec requests are refused",
      "env": "MAX_SESSIONS",
      "default": 8
    },
    "sessions-per-account": {
      "type": "number",
      "description": "Concurrent provisioning runs allowed for one account",
      "env": "SESSIONS_PER_ACCOUNT",
      "default": 2
    },
    "max-connections": {
      "type": "number",
      "description": "Concurrent SSH connections accepted",
      "env": "MAX_CONNECTIONS",
      "default": 200
    },
    "session-max-sec": {
      "type": "number",
      "description": "Hard ceiling on one provisioning run, after which it is terminated",
      "env": "SESSION_MAX_SEC",
      "default": 3600
    },
    "oauth-client-id": {
      "type": "string",
      "description": "Fallback client_id to refresh as when a deposited session does not carry its own. Must be the exact URL the authorization server fetches, e.g. https://<host>/oauth-client-metadata.json. Normally unnecessary: the page sends the client_id its session was issued to.",
      "env": "OAUTH_CLIENT_ID"
    },
    "ingress-proxy-host": {
      "type": "string",
      "description": "XRPC relay dispatcher host the requester registers with and discovers bidders through",
      "env": "INGRESS_PROXY_HOST",
      "default": "xrpc.fedproxy.com"
    },
    "relay-url": {
      "type": "string",
      "description": "Comma-separated ATProto relay URLs for firehose bid and event discovery",
      "env": "RELAY_URL"
    }
  }
}
